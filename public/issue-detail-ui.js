// Baixas / Consumo detalhado - base enriquecida por CR, placa e veiculo
(function(){
  function start(){
  if(document.documentElement.dataset.issueDetailUi==='1')return true;
  const D=window.ISSUE_DETAIL;if(!D||!Array.isArray(D.rows)||!D.rows.length)return false;
  document.documentElement.dataset.issueDetailUi='1';
  const dict=D.dict||{};
  const d=(name,i)=>dict[name]?.[Number(i)||0]||'';
  const norm=s=>String(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toUpperCase().trim();
  const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const br=v=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(v)||0);
  const n0=v=>new Intl.NumberFormat('pt-BR',{maximumFractionDigits:0}).format(Number(v)||0);
  const n1=v=>new Intl.NumberFormat('pt-BR',{minimumFractionDigits:0,maximumFractionDigits:1}).format(Number(v)||0);
  const pct=v=>new Intl.NumberFormat('pt-BR',{style:'percent',minimumFractionDigits:1,maximumFractionDigits:1}).format(Number(v)||0);
  const short=(s,n=24)=>String(s||'').length>n?String(s).slice(0,n-1)+'…':String(s||'');
  const dateText=k=>{const s=String(k||'');return s.length===8?`${s.slice(6,8)}/${s.slice(4,6)}/${s.slice(0,4)}`:''};
  const dateInput=k=>{const s=String(k||'');return s.length===8?`${s.slice(0,4)}-${s.slice(4,6)}-${s.slice(6,8)}`:''};
  const inputKey=s=>Number(String(s||'').replace(/-/g,''))||0;
  const rows=D.rows.map(r=>({
    date:Number(r[0])||0,unidade:d('unidade',r[1]),code:String(r[2]??''),product:d('product',r[3]),qty:Number(r[4])||0,unit:d('unit',r[5]),unitCost:Number(r[6])||0,cost:Number(r[7])||0,
    nro:d('nro',r[8]),company:d('company',r[9]),lot:d('lot',r[10]),group:d('group',r[11]),cr:d('cr',r[12]),localCode:d('local_code',r[13]),local:d('local',r[14]),area:d('area',r[15]),
    phaseCode:d('phase_code',r[16]),phase:d('phase',r[17]),status:d('status',r[18]),plate:d('plate',r[19]),vehicleCode:d('vehicle_code',r[20]),vehicle:d('vehicle',r[21])
  }));
  const style=document.createElement('style');style.id='issue-detail-ui-style';style.textContent=`
    #view-baixas .issue-hero{position:relative;overflow:hidden;margin-bottom:10px;padding:17px 19px;border-radius:14px;background:linear-gradient(118deg,var(--db-green-deep,#0d4726),var(--db-green-strong,#16733b) 65%,var(--db-green,#1e8747));color:#fff;box-shadow:0 9px 24px rgba(13,71,38,.17)}#view-baixas .issue-hero:after{content:'';position:absolute;right:0;top:0;width:135px;height:6px;background:var(--db-yellow,#faca04)}#view-baixas .issue-hero h2{margin:0 0 5px;font-size:18px;color:#fff}#view-baixas .issue-hero p{margin:0;font-size:10px;line-height:1.5;color:rgba(255,255,255,.92)}#view-baixas .issue-source{display:inline-flex;margin-bottom:7px;padding:4px 7px;border-radius:999px;background:var(--db-yellow,#faca04);color:var(--db-green-deep,#0d4726);font-size:8.5px;font-weight:900;text-transform:uppercase;letter-spacing:.035em}
    #view-baixas .issue-filters{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px;margin-bottom:10px;padding:11px;background:#fff;border:1px solid var(--db-line,#d8e4dc);border-radius:14px;box-shadow:var(--db-shadow,0 7px 24px rgba(17,92,49,.09))}#view-baixas .issue-filter{min-width:0;padding:8px 9px;border-radius:9px;background:#f6faf7;border:1px solid #e4eee7}#view-baixas .issue-filter label{display:block;margin-bottom:4px;font-size:8px;font-weight:900;text-transform:uppercase;color:var(--db-green-dark,#125c31)}#view-baixas .issue-filter input,#view-baixas .issue-filter select{width:100%;box-sizing:border-box;border:1px solid #d7e3db;background:#fff;border-radius:7px;padding:7px 8px;font-size:9.5px;color:#2a2a29}#view-baixas .issue-filter input:focus,#view-baixas .issue-filter select:focus{outline:2px solid rgba(30,135,71,.15);border-color:var(--db-green,#1e8747)}#view-baixas .issue-filter-actions{display:flex;align-items:end;gap:7px}#view-baixas .issue-reset{width:100%;border:1px solid #cfe0d5;background:#fff;color:var(--db-green-dark,#125c31);border-radius:8px;padding:8px 10px;font-size:9px;font-weight:900;cursor:pointer}#view-baixas .issue-reset:hover{background:var(--db-yellow-soft,#fff6c7);border-color:#efd76b}
    #view-baixas .issue-kpis{display:grid;grid-template-columns:repeat(5,1fr);gap:8px;margin-bottom:10px}#view-baixas .issue-kpi{position:relative;overflow:hidden;background:#fff;border:1px solid var(--db-line,#d8e4dc);border-radius:12px;padding:10px 11px;box-shadow:0 4px 15px rgba(17,92,49,.05)}#view-baixas .issue-kpi:before{content:'';position:absolute;left:0;top:0;bottom:0;width:4px;background:var(--db-green,#1e8747)}#view-baixas .issue-kpi:nth-child(2n):before{background:var(--db-yellow,#faca04)}#view-baixas .issue-kpi .k{font-size:8px;font-weight:900;text-transform:uppercase;color:#68746d}#view-baixas .issue-kpi .v{margin:4px 0 2px;font-size:18px;font-weight:900;color:var(--db-green-deep,#0d4726)}#view-baixas .issue-kpi .s{font-size:8.5px;color:#7a847e;line-height:1.35}
    #view-baixas .issue-panel{margin-bottom:10px;background:#fff;border:1px solid var(--db-line,#d8e4dc);border-radius:14px;overflow:hidden;box-shadow:var(--db-shadow,0 7px 24px rgba(17,92,49,.09))}#view-baixas .issue-panel-head{display:flex;align-items:flex-start;justify-content:space-between;gap:14px;padding:11px 13px;border-bottom:1px solid #e4ece7;background:linear-gradient(90deg,#fff,#f7fbf8)}#view-baixas .issue-panel-head h3{margin:0 0 3px;font-size:13px;color:var(--db-green-deep,#0d4726)}#view-baixas .issue-panel-head p{margin:0;font-size:8.8px;color:#718078}#view-baixas .issue-panel-total{text-align:right;font-size:9px;color:#68766e;white-space:nowrap}#view-baixas .issue-panel-total b{display:block;color:var(--db-green-deep,#0d4726);font-size:12px}#view-baixas .issue-breadcrumb{display:flex;gap:5px;align-items:center;flex-wrap:wrap;margin-top:7px}#view-baixas .issue-crumb{border:0;background:transparent;padding:3px 4px;border-radius:6px;color:var(--db-green-dark,#125c31);font-size:8.5px;font-weight:900;cursor:pointer}#view-baixas .issue-crumb:hover{background:var(--db-green-soft,#e6f3eb)}#view-baixas .issue-crumb.current{color:#6e7972;cursor:default}#view-baixas .issue-sep{color:#a2aaa5;font-weight:900}
    #view-baixas .issue-chart-scroll{overflow-x:auto;overflow-y:hidden;padding:13px 10px 8px;scrollbar-color:#b8ccbe #eef4f0;scrollbar-width:thin}#view-baixas .issue-chart{position:relative;height:355px;width:100%}#view-baixas .issue-chart-empty{height:300px;display:grid;place-items:center;color:#738078;font-size:10px}#view-baixas .issue-gridline{position:absolute;left:0;right:0;border-top:1px dashed #e4ece7;font-size:7.5px;color:#87928b}#view-baixas .issue-gridline span{position:absolute;left:0;top:-10px;background:#fff;padding-right:5px}#view-baixas .issue-bars{position:absolute;left:62px;right:8px;top:15px;bottom:73px;display:flex;align-items:flex-end;gap:13px}#view-baixas .issue-bar-item{height:100%;flex:0 0 92px;display:flex;flex-direction:column;justify-content:flex-end;align-items:stretch;cursor:pointer;outline:none}#view-baixas .issue-bar-slot{height:calc(100% - 1px);display:flex;flex-direction:column;justify-content:flex-end;align-items:center}#view-baixas .issue-bar-value{font-size:8px;font-weight:900;color:var(--db-green-deep,#0d4726);white-space:nowrap;margin-bottom:4px}#view-baixas .issue-bar{position:relative;width:62px;min-height:4px;border-radius:5px 5px 2px 2px;background:linear-gradient(180deg,var(--db-green,#1e8747),var(--db-green-dark,#125c31));box-shadow:0 2px 7px rgba(17,92,49,.14);transition:filter .12s ease,transform .12s ease}#view-baixas .issue-bar:before{content:'';position:absolute;left:0;right:0;top:0;height:5px;border-radius:5px 5px 0 0;background:var(--db-yellow,#faca04)}#view-baixas .issue-bar-item:hover .issue-bar,#view-baixas .issue-bar-item:focus .issue-bar{filter:brightness(1.08);transform:translateY(-2px)}#view-baixas .issue-bar-label{height:38px;margin-top:6px;text-align:center;font-size:7.8px;font-weight:850;line-height:1.18;color:#314038;overflow:hidden}#view-baixas .issue-bar-meta{text-align:center;font-size:7px;color:#839087;white-space:nowrap}#view-baixas .issue-chart-note{padding:0 12px 10px;font-size:8px;color:#7b877f}
    #view-baixas .issue-detail-wrap{max-height:420px;overflow:auto}#view-baixas .issue-detail-table{width:100%;border-collapse:collapse;font-size:8.8px}#view-baixas .issue-detail-table th{position:sticky;top:0;z-index:1;padding:7px 8px;background:var(--db-green-deep,#0d4726);color:#fff;text-align:left}#view-baixas .issue-detail-table td{padding:7px 8px;border-bottom:1px solid #edf2ef;vertical-align:top}#view-baixas .issue-detail-table tr:hover{background:#edf8f1}#view-baixas .issue-detail-table .num{text-align:right;white-space:nowrap}#view-baixas .issue-muted{color:#7b8780;font-size:7.8px}
    @media(max-width:1150px){#view-baixas .issue-filters{grid-template-columns:repeat(2,minmax(0,1fr))}#view-baixas .issue-kpis{grid-template-columns:repeat(2,1fr)}}@media(max-width:700px){#view-baixas .issue-filters{grid-template-columns:1fr}#view-baixas .issue-kpis{grid-template-columns:1fr}#view-baixas .issue-panel-head{flex-direction:column}#view-baixas .issue-panel-total{text-align:left}.issue-detail-table{min-width:900px}}
  `;document.head.appendChild(style);
  function install(){
    const tab=document.querySelector('.tab[data-view="baixas"]');let view=document.getElementById('view-baixas');if(!tab||!view)return false;tab.textContent='Baixas / Consumo';const clean=document.querySelector('.clean-sub-btn[data-view="baixas"]');if(clean)clean.textContent='Baixas / Consumo';if(view.querySelector('[data-issue-detail-root="1"]'))return true;
    view.innerHTML=`<div data-issue-detail-root="1"><div class="issue-hero"><div class="issue-source">Base detalhada de baixas</div><h2>Baixas / Consumo — onde estamos gastando</h2><p>Consumo detalhado por local, centro de resultado, placa, código/veículo, grupo e produto. O valor de gasto desta análise usa o campo <b>Custo_Total</b> da nova planilha.</p></div><div class="issue-filters"><div class="issue-filter"><label>Data inicial</label><input type="date" id="issueFrom" value="${dateInput(D.summary?.from)}"></div><div class="issue-filter"><label>Data final</label><input type="date" id="issueTo" value="${dateInput(D.summary?.to)}"></div><div class="issue-filter"><label>Local / Almoxarifado</label><select id="issueLocal"><option value="">Todos os locais</option></select></div><div class="issue-filter"><label>Centro de resultado (CR)</label><input id="issueCr" list="issueCrList" placeholder="Digite ou escolha o CR"><datalist id="issueCrList"></datalist></div><div class="issue-filter"><label>Placa</label><input id="issuePlate" list="issuePlateList" placeholder="Ex.: RMO5H33"><datalist id="issuePlateList"></datalist></div><div class="issue-filter"><label>Código / Veículo</label><input id="issueVehicle" list="issueVehicleList" placeholder="Ex.: 05-624 ou descrição"><datalist id="issueVehicleList"></datalist></div><div class="issue-filter"><label>Grupo</label><select id="issueGroup"><option value="">Todos os grupos</option></select></div><div class="issue-filter"><label>Produto / Código</label><input id="issueProduct" placeholder="Buscar produto ou código"></div><div class="issue-filter"><label>Área</label><select id="issueArea"><option value="">Todas as áreas</option></select></div><div class="issue-filter"><label>Unidade</label><select id="issueUnit"><option value="">Todas as unidades</option></select></div><div class="issue-filter issue-filter-actions"><button type="button" class="issue-reset" id="issueReset">Limpar filtros</button></div></div><div class="issue-kpis" id="issueKpis"></div><div class="issue-panel"><div class="issue-panel-head"><div><h3>Onde estamos gastando</h3><p>Clique nas colunas para aprofundar: Local → Centro de resultado → Veículo/Placa → Grupo → Produto.</p><div class="issue-breadcrumb" id="issueBreadcrumb"></div></div><div class="issue-panel-total"><span id="issueLevelText"></span><b id="issueChartTotal"></b></div></div><div class="issue-chart-scroll"><div class="issue-chart" id="issueChart"></div></div><div class="issue-chart-note" id="issueChartNote"></div></div><div class="issue-panel"><div class="issue-panel-head"><div><h3>Detalhamento das baixas</h3><p>Registros da planilha conforme os filtros atuais.</p></div><div class="issue-panel-total"><span id="issueDetailCount"></span><b id="issueDetailTotal"></b></div></div><div class="issue-detail-wrap"><table class="issue-detail-table"><thead><tr><th>Data</th><th>Local</th><th>Centro de resultado</th><th>Veículo / placa</th><th>Produto</th><th class="num">Qtd.</th><th class="num">Custo</th></tr></thead><tbody id="issueDetailBody"></tbody></table></div></div></div>`;
    const $=s=>view.querySelector(s);const fillSelect=(el,field)=>{const values=[...new Set(rows.map(r=>r[field]).filter(Boolean))].sort((a,b)=>a.localeCompare(b,'pt-BR'));el.insertAdjacentHTML('beforeend',values.map(v=>`<option value="${esc(v)}">${esc(v)}</option>`).join(''));};fillSelect($('#issueLocal'),'local');fillSelect($('#issueGroup'),'group');fillSelect($('#issueArea'),'area');fillSelect($('#issueUnit'),'unidade');const fillList=(id,vals)=>{$(id).innerHTML=[...new Set(vals.filter(Boolean))].sort((a,b)=>a.localeCompare(b,'pt-BR')).map(v=>`<option value="${esc(v)}"></option>`).join('')};fillList('#issueCrList',rows.map(r=>r.cr));fillList('#issuePlateList',rows.map(r=>r.plate));fillList('#issueVehicleList',rows.flatMap(r=>[r.vehicleCode,r.vehicle]).filter(Boolean));
    let path={local:'',cr:'',vehicleKey:'',vehicleLabel:'',group:''};let filtered=[];const fields={from:$('#issueFrom'),to:$('#issueTo'),local:$('#issueLocal'),cr:$('#issueCr'),plate:$('#issuePlate'),vehicle:$('#issueVehicle'),group:$('#issueGroup'),product:$('#issueProduct'),area:$('#issueArea'),unit:$('#issueUnit')};const vehicleKey=r=>[r.vehicleCode,r.plate,r.vehicle].join('|');const vehicleLabel=r=>r.vehicleCode&&r.plate?`${r.vehicleCode} • ${r.plate}`:r.vehicleCode||r.plate||(r.vehicle?short(r.vehicle,30):'SEM VEÍCULO / PLACA');const vehicleFull=r=>[r.vehicleCode,r.plate,r.vehicle].filter(Boolean).join(' • ')||'Sem veículo / placa';
    function filterRows(){const from=inputKey(fields.from.value),to=inputKey(fields.to.value)||99999999,cr=norm(fields.cr.value),plate=norm(fields.plate.value),veh=norm(fields.vehicle.value),prod=norm(fields.product.value),local=fields.local.value,group=fields.group.value,area=fields.area.value,unidade=fields.unit.value;return rows.filter(r=>r.date>=from&&r.date<=to&&(!local||r.local===local)&&(!group||r.group===group)&&(!area||r.area===area)&&(!unidade||r.unidade===unidade)&&(!cr||norm(r.cr).includes(cr))&&(!plate||norm(r.plate).includes(plate))&&(!veh||norm(r.vehicleCode).includes(veh)||norm(r.vehicle).includes(veh))&&(!prod||norm(r.code).includes(prod)||norm(r.product).includes(prod)))}
    function contextRows(){return filtered.filter(r=>(!path.local||r.local===path.local)&&(!path.cr||r.cr===path.cr)&&(!path.vehicleKey||vehicleKey(r)===path.vehicleKey)&&(!path.group||r.group===path.group))}function currentLevel(){return !path.local?'local':!path.cr?'cr':!path.vehicleKey?'vehicle':!path.group?'group':'product'}const levelName=l=>({local:'Local',cr:'Centro de resultado',vehicle:'Veículo / placa',group:'Grupo',product:'Produto'})[l]||l;
    function aggregate(level,src){const map=new Map();for(const r of src){let key='',label='',meta='';if(level==='local'){key=r.local||'SEM LOCAL';label=key;meta=r.localCode?`Cód. ${r.localCode}`:''}else if(level==='cr'){key=r.cr||'SEM CR';label=key;meta=r.area||''}else if(level==='vehicle'){key=vehicleKey(r);label=vehicleLabel(r);meta=vehicleFull(r)}else if(level==='group'){key=r.group||'SEM GRUPO';label=key}else{key=r.code+'|'+r.product;label=r.code+' - '+r.product;meta=r.unit||''}let a=map.get(key);if(!a){a={key,label,meta,value:0,count:0,qty:0,code:r.code,product:r.product,unit:r.unit};map.set(key,a)}a.value+=r.cost;a.count++;a.qty+=r.qty}return [...map.values()].sort((a,b)=>b.value-a.value||String(a.label).localeCompare(String(b.label),'pt-BR'))}
    function renderKpis(){const total=filtered.reduce((a,r)=>a+r.cost,0),products=new Set(filtered.map(r=>r.code)),crs=new Set(filtered.map(r=>r.cr).filter(Boolean)),vehicleRows=filtered.filter(r=>r.vehicle||r.plate||r.vehicleCode).length,vehicles=new Set(filtered.filter(r=>r.vehicle||r.plate||r.vehicleCode).map(vehicleKey));$('#issueKpis').innerHTML=`<div class="issue-kpi"><div class="k">Custo consumido</div><div class="v">${br(total)}</div><div class="s">Campo Custo_Total das baixas filtradas.</div></div><div class="issue-kpi"><div class="k">Registros de baixa</div><div class="v">${n0(filtered.length)}</div><div class="s">${dateText(inputKey(fields.from.value))} a ${dateText(inputKey(fields.to.value))}</div></div><div class="issue-kpi"><div class="k">Produtos</div><div class="v">${n0(products.size)}</div><div class="s">Códigos consumidos no filtro.</div></div><div class="issue-kpi"><div class="k">Centros de resultado</div><div class="v">${n0(crs.size)}</div><div class="s">Onde o gasto foi apropriado.</div></div><div class="issue-kpi"><div class="k">Veículos identificados</div><div class="v">${n0(vehicles.size)}</div><div class="s">${pct(filtered.length?vehicleRows/filtered.length:0)} das baixas com veículo/placa.</div></div>`}
    function breadcrumb(){const parts=[`<button class="issue-crumb ${!path.local?'current':''}" data-back="local">Local</button>`];if(path.local)parts.push('<span class="issue-sep">›</span>',`<button class="issue-crumb ${!path.cr?'current':''}" data-back="cr">${esc(path.local)}</button>`);if(path.cr)parts.push('<span class="issue-sep">›</span>',`<button class="issue-crumb ${!path.vehicleKey?'current':''}" data-back="vehicle">${esc(short(path.cr,34))}</button>`);if(path.vehicleKey)parts.push('<span class="issue-sep">›</span>',`<button class="issue-crumb ${!path.group?'current':''}" data-back="group">${esc(short(path.vehicleLabel,28))}</button>`);if(path.group)parts.push('<span class="issue-sep">›</span>',`<button class="issue-crumb current" data-back="product">${esc(path.group)}</button>`);$('#issueBreadcrumb').innerHTML=parts.join('')}
    function renderChart(){const src=contextRows(),level=currentLevel(),items=aggregate(level,src),total=src.reduce((a,r)=>a+r.cost,0),max=items[0]?.value||0;breadcrumb();$('#issueLevelText').textContent=levelName(level);$('#issueChartTotal').textContent=br(total);if(!items.length){$('#issueChart').innerHTML='<div class="issue-chart-empty">Nenhuma baixa encontrada para este filtro.</div>';$('#issueChartNote').textContent='';return}const chart=$('#issueChart');chart.style.minWidth=Math.max(720,62+items.length*105)+'px';const lines=[1,.75,.5,.25,0].map(p=>`<div class="issue-gridline" style="top:${15+(1-p)*267}px"><span>${br(max*p)}</span></div>`).join('');const bars=items.map((x,i)=>{const h=max?Math.max(4,Math.round((x.value/max)*245)):4,meta=level==='product'?`${n1(x.qty)} ${esc(x.unit||'')} • ${x.count} baixa${x.count===1?'':'s'}`:`${x.count} baixa${x.count===1?'':'s'}`;return `<div class="issue-bar-item" tabindex="0" role="button" data-index="${i}" title="${esc(x.label)} — ${br(x.value)}"><div class="issue-bar-slot"><div class="issue-bar-value">${br(x.value)}</div><div class="issue-bar" style="height:${h}px"></div></div><div class="issue-bar-label">${esc(short(x.label,34))}</div><div class="issue-bar-meta">${meta}</div></div>`}).join('');chart.innerHTML=lines+`<div class="issue-bars">${bars}</div>`;$('#issueChartNote').textContent=`${items.length} ${levelName(level).toLowerCase()}${items.length===1?'':'s'} • maior coluna ${br(max)} • use a rolagem horizontal para consultar todas as categorias.`;chart._items=items}
    function renderDetail(){const src=contextRows().slice().sort((a,b)=>b.date-a.date||b.cost-a.cost),total=src.reduce((a,r)=>a+r.cost,0),show=src.slice(0,120);$('#issueDetailCount').textContent=`${n0(src.length)} registros${src.length>120?' • 120 mais recentes exibidos':''}`;$('#issueDetailTotal').textContent=br(total);$('#issueDetailBody').innerHTML=show.map(r=>`<tr><td>${dateText(r.date)}</td><td>${esc(r.local)}<div class="issue-muted">${esc(r.unidade)}</div></td><td>${esc(r.cr)}<div class="issue-muted">${esc(r.area||'')}</div></td><td>${esc(vehicleFull(r))}</td><td><b>${esc(r.code)}</b> - ${esc(r.product)}<div class="issue-muted">${esc(r.group)}</div></td><td class="num">${n1(r.qty)} ${esc(r.unit)}</td><td class="num"><b>${br(r.cost)}</b></td></tr>`).join('')}
    function renderAll(resetPath=false){if(resetPath)path={local:'',cr:'',vehicleKey:'',vehicleLabel:'',group:''};filtered=filterRows();renderKpis();renderChart();renderDetail()}for(const el of Object.values(fields))el.addEventListener(el.tagName==='INPUT'?'input':'change',()=>renderAll(true));$('#issueReset').addEventListener('click',()=>{fields.from.value=dateInput(D.summary?.from);fields.to.value=dateInput(D.summary?.to);fields.local.value='';fields.cr.value='';fields.plate.value='';fields.vehicle.value='';fields.group.value='';fields.product.value='';fields.area.value='';fields.unit.value='';renderAll(true)});$('#issueBreadcrumb').addEventListener('click',e=>{const b=e.target.closest('[data-back]');if(!b)return;const l=b.dataset.back;if(l==='local')path={local:'',cr:'',vehicleKey:'',vehicleLabel:'',group:''};else if(l==='cr')path={...path,cr:'',vehicleKey:'',vehicleLabel:'',group:''};else if(l==='vehicle')path={...path,vehicleKey:'',vehicleLabel:'',group:''};else if(l==='group')path={...path,group:''};renderChart();renderDetail()});
    function openStock(local,group,code){const set=(id,value)=>{const el=document.getElementById(id);if(!el)return;let v=String(value||'');if(el.tagName==='SELECT'){const nv=norm(v),o=[...el.options].find(x=>norm(x.value)===nv||norm(x.textContent)===nv);if(o)v=o.value}el.value=v;el.dispatchEvent(new Event(el.tagName==='INPUT'?'input':'change',{bubbles:true}))};for(const id of ['fEmpresa','fLocal','fGrupo','fUn','fFornecedor','fBusca'])set(id,'');set('fLocal',local);set('fGrupo',group);set('fBusca',code);document.querySelector('.tab[data-view="estoque"]')?.click();setTimeout(()=>{set('fLocal',local);set('fGrupo',group);set('fBusca',code);window.scrollTo({top:0,behavior:'smooth'})},70)}
    const drill=e=>{const bar=e.target.closest('.issue-bar-item');if(!bar)return;if(e.type==='keydown'&&e.key!=='Enter'&&e.key!==' ')return;if(e.type==='keydown')e.preventDefault();const items=$('#issueChart')._items||[],x=items[Number(bar.dataset.index)];if(!x)return;const level=currentLevel();if(level==='local')path.local=x.key;else if(level==='cr')path.cr=x.key;else if(level==='vehicle'){path.vehicleKey=x.key;path.vehicleLabel=x.label}else if(level==='group')path.group=x.key;else openStock(path.local,path.group,x.code);if(level!=='product'){renderChart();renderDetail()}};$('#issueChart').addEventListener('click',drill);$('#issueChart').addEventListener('keydown',drill);filtered=filterRows();renderKpis();renderChart();renderDetail();return true;
  }
  if(!install()){let tries=0;const timer=setInterval(()=>{tries++;if(install()||tries>60)clearInterval(timer)},100)}return true;
  }
  if(!start())window.addEventListener('issue-detail-ready',start,{once:true});
})();
